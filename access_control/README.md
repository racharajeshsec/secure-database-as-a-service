## Access Control Module

This module enforces role-based access control (RBAC).

Two roles are supported:
- Group H: Full access to all patient data
- Group R: Restricted read-only access without first and last names

Access permissions are enforced before query results are returned to the user.

